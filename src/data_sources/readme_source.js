import resume_data from '../data_sources/resume_source';

let readme_data = {

  headline: "About app",
  info: "It's an app, which written in react; the main idea was a dynamicly renderenig any datastructure. So now, app recursive render `js-objects` like item below.",
  jsonify_example: JSON.stringify(resume_data)

};

export default readme_data;
